# Setup path
export PATH=$HOME/.bin:/usr/local/opt/gnu-tar/libexec/gnubin:/usr/local/bin:$PATH

# Load z
. ~/.dotfiles/configs/zsh/z/z.sh

# Source a local .env file if it exists
[ -f ~/.env ] && source ~/.env

# Load fzf
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
